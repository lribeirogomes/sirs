<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />

    <size android:height="@dimen/box_min_size"/>

    <!-- view background color -->
    <solid
        android:color="@color/lighterBackground"/>

    <!-- view border color and width -->
    <stroke
        android:width="0dp"
        android:color="@color/lighterBackground" />

    <!-- The radius makes the corners rounded -->
    <corners
        android:radius="0dp"   />

</shape>